import { DocumentScannerConfiguration } from "./document-scanner-configuration";
import { WorkerBridge } from "../worker/worker-bridge";
export interface ScanbotCameraProps {
    configuration: DocumentScannerConfiguration;
    bridge: WorkerBridge;
    container?: HTMLElement;
    onReady?: (documentScannerView: any) => void;
    onError?: (err: Error) => void;
}
