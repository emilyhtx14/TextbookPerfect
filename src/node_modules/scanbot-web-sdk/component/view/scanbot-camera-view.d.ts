import { h } from "preact";
import React from "preact/compat";
export interface ScanbotCameraViewProps {
    videoConstraints: MediaTrackConstraints;
    onReady: () => void;
    onError: (err: Error) => void;
}
export declare class ScanbotCameraView extends React.Component<ScanbotCameraViewProps, any> {
    stream: MediaStream | null;
    video: HTMLVideoElement | null;
    videoObjectUrl?: string;
    canvas: HTMLCanvasElement | null;
    ctx: CanvasRenderingContext2D | null;
    constructor(props: any);
    createImageData(maxSmallerSize?: number): ImageData;
    calculateFrameSize(maxSmallerSize: number): {
        frameWidth: number;
        frameHeight: number;
    };
    convertToViewportPoints(points: any[], maxSmallerSize: number): {
        x: number;
        y: number;
    }[];
    getCanvas(): HTMLCanvasElement;
    _requestUserMedia(): Promise<any>;
    componentDidMount(): Promise<void>;
    componentWillUnmount(): void;
    render(): h.JSX.Element;
}
