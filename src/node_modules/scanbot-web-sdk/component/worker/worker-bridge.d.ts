export declare class WorkerBridge {
    ASM_JS: string;
    instance: any | null;
    continuations: any;
    static initialize(path: string): Promise<WorkerBridge>;
    post(command: string, args: any[]): Promise<any>;
    isRemote(path: string): boolean;
}
